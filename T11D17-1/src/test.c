#include <stdio.h>

int main() {

    int x;
    char ch;

    while(scanf("%d", &x) == 1 && x != -1) {
        
    }

    ch = getchar();
    if (ch != ' ' && ch != '\n')
        printf("error.\n");



    printf("%d\n", x);

}